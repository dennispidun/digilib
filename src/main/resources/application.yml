spring:
  h2:
    console:
      path: /h2-console
  jpa:
    hibernate:
      ddl-auto: update
  datasource:
    url: jdbc:h2:~/digilib;DB_CLOSE_ON_EXIT=FALSE
    username: sa
    password:
    driver-class-name: org.h2.Driver
security:
  ignored:
    - "*.bundle.*"


